pipeline {
    agent any
    stages {
        stage ('Build') {
            steps {
                // Use Maven to build the Java code
                sh 'mvn package'
            }
        }
    }
}
  
